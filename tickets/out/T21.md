# T21 release report

Date: 2026-09-20 (Asia/Kolkata)

Status: **NO-GO for deployed release; local implementation is release-ready**

## Local release gate

- Backend: `PYTHONPATH=. .venv/bin/pytest -q` — 60 passed, 1 skipped. The skip is the Linux-only kernel restriction check on macOS and still requires Linux CI evidence.
- Frontend contracts: `npm test` — 4 passed.
- Frontend production/mounted flows: `npm run test:app` — production build passed and 12 tests passed.
- Production assets: 551.67 kB JavaScript (183.33 kB gzip) and 31.39 kB CSS (5.77 kB gzip). Vite reports the expected >500 kB chunk warning from the locally bundled CodeMirror editor.
- Infrastructure: `sam validate --lint` passed; `sam build` passed.
- Repository hygiene: `git diff --check` passed.

## Browser and visual evidence

Local Chrome 153 mock-API review captured home, baseline, and populated practice at 1440x900, 1280x720, and 390x844. `tickets/out/ui-review/browser-review.json` records matching viewport and document widths for all nine captures, so no horizontal page overflow was observed. The practice capture shows the requested 45/55 question-left/editor-right workspace, trace anchored bottom-left, a locally bundled Python editor, reachable Run & Check, and a visible verdict. Fault injection verified a network error followed by successful retry with code preserved; direct practice reload, hint disclosure, trace refresh, and keyboard focus were exercised.

## Deployed-release blockers

1. The current live executor was reproduced failing before learner code because AWS Lambda rejects installation of the nested seccomp filter. T16 implements and tests a Lambda-only fallback while retaining the private no-route/no-DNS function, least-privilege role, stripped environment/descriptors, resource caps, denied file opens, and blocked process/network imports. Deploying this security-boundary change to the shared stack requires explicit approval; no stack mutation occurred.
2. The new frontend is local only until the repository owner pushes the commits. Amplify is connected to `main` and will build that pushed revision; Codex did not push.
3. Consequently, hosted-origin baseline/practice submission, live audio upload/playback/delete, deployed trace toggling, and the final Amplify revision cannot honestly be marked passed yet. No extra public quota was spent and no quota was reset.

## Release decision

**NO-GO for claiming a deployed release.** The code, automated checks, and local visual workflow are ready. After explicit approval, deploy the existing `compile-v2` stack, validate the private executor, run at most one coordinated public practice retry, push `main`, wait for Amplify success, and perform the hosted browser/audio checks before changing this decision to GO.

## End-of-run report

- Files changed: `frontend/test/app.test.js`, `README.md`, `tickets/out/T21.md`, `tickets/out/ui-review/*`.
- Interfaces produced (actual): no product interfaces; dated local verification and release decision only.
- New interfaces introduced: none.
- Blockers / deviations from plan: live backend deployment requires explicit approval; frontend release requires repository-owner push; Linux CI and hosted browser/audio checks remain pending.
