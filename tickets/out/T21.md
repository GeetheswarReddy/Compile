# T21 release report

Date: 2026-09-20 (Asia/Kolkata)

Status: **Backend GO; complete hosted release pending frontend push**

## Local release gate

- Backend: `PYTHONPATH=. .venv/bin/pytest -q` — 60 passed, 1 skipped. The skip is the Linux-only kernel restriction check on macOS and still requires Linux CI evidence.
- Frontend contracts: `npm test` — 4 passed.
- Frontend production/mounted flows: `npm run test:app` — production build passed and 12 tests passed.
- Editor follow-up: Tab/Shift+Tab indentation and explicit pass/fail result copy are covered by the final 12-test mounted suite.
- Production assets: 551.67 kB JavaScript (183.33 kB gzip) and 31.39 kB CSS (5.77 kB gzip). Vite reports the expected >500 kB chunk warning from the locally bundled CodeMirror editor.
- Infrastructure: `sam validate --lint` passed; `sam build` passed.
- Repository hygiene: `git diff --check` passed.

## Browser and visual evidence

Local Chrome 153 mock-API review captured home, baseline, and populated practice at 1440x900, 1280x720, and 390x844. `tickets/out/ui-review/browser-review.json` records matching viewport and document widths for all nine captures, so no horizontal page overflow was observed. The practice capture shows the requested 45/55 question-left/editor-right workspace, trace anchored bottom-left, a locally bundled Python editor, reachable Run & Check, and a visible verdict. Fault injection verified a network error followed by successful retry with code preserved; direct practice reload, hint disclosure, trace refresh, and keyboard focus were exercised.

## Deployed backend evidence

- The user explicitly approved the disclosed Lambda-only fallback. The existing `compile-v2` stack updated in place and reached `UPDATE_COMPLETE` at `2026-09-20T14:17:00.974000+00:00` with no planned replacements.
- Direct private sandbox verification passed a known-good seeded reference solution without a Lambda function error.
- One coordinated public practice retry passed: completed-baseline resume, question selection, Run & Check (`passed: true`, `scored: true`), hint level 1, Demo Trace, and CORS/error readability.
- The synthetic learner moved from 1 to 2 practice checks. Shared execution usage is 10/40, leaving 30; generation remains zero/unset. No quota was reset.

## Remaining release blocker

The redesigned frontend is local until the repository owner pushes `main`. Amplify must build that revision before the hosted-origin visual, navigation/reload, and synthetic audio upload/playback/delete checks can be completed. Codex did not push.

## Release decision

**GO for the backend and for pushing the frontend.** Push `main`, wait for Amplify success, then perform the hosted browser/navigation/audio checks before claiming the complete deployed release.

## End-of-run report

- Files changed: `frontend/test/app.test.js`, `README.md`, `tickets/out/T21.md`, `tickets/out/ui-review/*`.
- Interfaces produced (actual): no product interfaces; dated local verification and release decision only.
- New interfaces introduced: none.
- Blockers / deviations from plan: frontend release requires the repository-owner push; Linux CI and hosted browser/audio checks remain pending.
