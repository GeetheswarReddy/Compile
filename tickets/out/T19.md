# T19 completion report

- Files changed: `frontend/src/App.jsx`, `frontend/src/practice/PracticeScreen.jsx`, `frontend/src/practice/practice.css`, `frontend/src/baseline/BaselineAssessment.jsx`, `frontend/src/baseline/baseline.css`, `frontend/src/trace/DemoTrace.jsx`, `frontend/src/trace/trace.css`, `frontend/test/workspace.test.js`, `tickets/out/T19.md`
- Interfaces produced (actual):
  - `PracticeScreen({ topicId, onQuestionIdChange, onAttemptChange, trace = null, support = null, history = null })` preserves the existing topic and callback props and adds the planned optional ReactNode slots. `trace` renders in the lower-left desktop grid cell; `support` and `history` render after the question content in the independently scrolling left pane. Below 900px their visual order is question, solution/actions/results, trace, then support/history.
  - `onQuestionIdChange(questionId: string)` still receives the active question ID and receives `''` during cleanup. `onAttemptChange(attempted: boolean)` reports whether the active question has a verdict.
  - `DemoTrace({ enabled })` retains its existing prop contract. When disabled it returns `null` and `useDemoTrace(false)` does not fetch. `PracticeComposition` remounts the enabled trace with key `${questionId}-${attempted}`, refreshing it when the active question or attempt state changes.
  - `BaselineAssessment()` retains its existing hook and submission contracts and now renders `.baseline-workspace` with `.baseline-question-pane` followed by `.baseline-solution-pane`.
  - Workspace CSS hooks produced: `.practice-workspace.has-trace`, `.practice-question-pane`, `.practice-trace-slot`, `.practice-solution-pane`, `.practice-editor-stage`, `.practice-support-region`, `.baseline-workspace`, `.baseline-question-pane`, and `.baseline-solution-pane`.
- New interfaces introduced: none beyond the planned optional `PracticeScreen` slots.
- Blockers / deviations from plan: no implementation blocker. Screenshots were captured to temporary `/tmp` files because the ticket does not own persistent image-artifact paths. No deployment verification was performed; T21 owns deployment/browser verification after T16.

## Behavior and integration notes

- Desktop practice uses the available viewport below the compact application header. The question/trace column and editor/results column use a 45/55 split. Question/support, trace, editor, and result regions each keep their own overflow boundary; Run & Check remains visible above results.
- A higher-specificity T18 compatibility rule had capped the practice workspace at 980px. T19 overrides it within `practice.css`, so the 45/55 panes now use the full viewport width up to 1560px. The same override clears obsolete two-column support padding without editing T18-owned `global.css`.
- With trace disabled, the trace cell is absent, no `/demo-trace` request is made, and the question pane occupies the full left-column height. With trace enabled, compact loading, empty, populated, and error states stay in the lower-left cell.
- Question content includes provenance, difficulty, prompt, public examples, function signature, supplied constraints, and the existing opt-in technique reveal. Hints stay in the left support region; reflection and history remain secondary disclosure sections.
- Results remain below the editor and expose at most two failed cases through the existing normalized verdict contract. Run & Check remains the only execution action.
- Baseline uses the same question-left/editor-right desktop arrangement and keeps its existing submit/error/quota behavior. It has no practice trace.

## Visual verification

- Rendered against an isolated local `/v1` mock with a generated question, two examples, constraints, a populated trace entry, and a failed verdict containing two displayed diagnostics. Captured and inspected exact 1440×900, 1280×720, and 390×844 screenshots.
- At 1440×900, the question/trace column measured 632px and the solution column 772px (45.0%/55.0% of their 1404px grid). Question occupied the upper-left cell, trace the lower-left cell, and editor/actions/result the full right column. Run & Check ended at y=670 and the result ended at y=871 inside the 900px viewport.
- At 1280×720, the columns measured 560px and 684px (45.0%/55.0% of their 1244px grid). Run & Check ended at y=490 and the result ended at y=691 inside the 720px viewport. The long question region scrolls independently while trace remains anchored below it.
- At 390×844, the rendered order was question, solution/actions/result, trace, then support/history. `innerWidth`, `documentElement.scrollWidth`, and `body.scrollWidth` were all exactly 390px; no horizontal page overflow was present. Run & Check remained reachable in normal vertical flow.
- Baseline was also inspected at the same three sizes: desktop remained side-by-side, mobile stacked question before solution, and all three viewports had matching inner and document widths.

## Verification

- `npm test` from `frontend/`: 4 passed.
- `npm run test:app` from `frontend/`: build passed and 10 mounted tests passed.
- `VITE_DEMO_TRACE=false npm run build && node --test test/workspace.test.js`: 2 passed, including the disabled trace/no-fetch path; the normal trace-enabled build was restored afterward.
- `frontend/test/workspace.test.js` covers loading, practice landmark/keyboard order, metadata/examples/constraints, populated trace, trace refresh/error after an attempt, disabled trace/no fetch, editor/action/result containment, two-case disclosure, and baseline landmark order. Existing mounted suites cover completion, recoverable errors, recording eligibility, and quota states.
- The workspace harness now provides the animation-frame, resize-observer, and range-geometry APIs required by CodeMirror. Its editor-region assertion follows T20's stable `.cm-content[role="textbox"][aria-label="Your Python solution"]` contract and confirms the node belongs to a live `EditorView`.
- `npm run build && node --test test/workspace.test.js` after T20 integration: build passed and both T19 workspace tests passed.
- `git diff --check` over all T19-owned changed files: passed.
