# T18 completion report

- Files changed: `frontend/src/App.jsx`, `frontend/src/styles/global.css`, `frontend/src/baseline/baseline.css`, `frontend/src/hints/HintLadder.jsx`, `frontend/src/reflection/reflection.css`, `tickets/out/T18.md`
- Interfaces produced (actual):
  - Shared color tokens: `--color-bg`, `--color-surface`, `--color-surface-raised`, `--color-surface-muted`, `--color-border`, `--color-border-strong`, `--color-text`, `--color-text-muted`, `--color-text-subtle`, `--color-accent`, `--color-accent-hover`, `--color-accent-contrast`, `--color-focus`, `--color-success`, `--color-success-surface`, `--color-warning`, `--color-warning-surface`, `--color-danger`, and `--color-danger-surface`.
  - Shared typography and layout tokens: `--font-sans`, `--font-mono`, `--space-1`, `--space-2`, `--space-3`, `--space-4`, `--space-5`, `--space-6`, `--space-8`, `--space-10`, `--radius-sm`, `--radius-md`, `--radius-lg`, `--control-height`, and `--header-height`.
  - Shared control contract: native buttons, inputs, textareas, selects, and links receive consistent font, disabled, hover, and `:focus-visible` treatment. Primary actions use the accent tokens; secondary actions use surface and border tokens; danger and status UI use the matching semantic tokens.
  - Shared page hooks: `.app-header`, `.app-header__inner`, `.app-brand`, `.app-header__descriptor`, `.topic-selection`, `.baseline-callout`, `.topic-grid`, `.topic-card`, `.app-loading`, and `.app-initialization-error` form the compact application shell and home page. Existing `.practice-support`, `.practice-history`, and `.question-examples` receive compatible temporary treatments for T19/T20 to consume or supersede.
  - Baseline keeps its existing component and hook contract. `.baseline-submit-error` is now a bordered, readable danger alert, and baseline loading, failure, completion, question, editor, focus, disabled, and submit states share the global tokens.
  - Hint and reflection behavior is unchanged. Each `.hint-ladder__step-state` now renders the text `Locked` or `Revealed`, so disclosure state is not conveyed by color alone.
- New interfaces introduced:
  - Internal `TopicSelection({ baselineCompleted })` prop, derived from the already-loaded learner, selects the baseline progress copy without changing routing or API contracts.
  - Internal `.hint-ladder__step-state` styling hook for the visible hint state label.
- Blockers / deviations from plan: none.

## Behavior and integration notes

- T17 initialization retry, baseline retry, duplicate-submit prevention, quota behavior, and preserved editor state remain unchanged. The exact navigation button text expected by the mounted tests is preserved; the brand mark and card arrow are CSS-generated decoration.
- T19 should use the shared color, spacing, radius, control-height, header-height, and typography tokens. It can replace the compatibility layout rules for `.practice-screen` and `.practice-support` when it introduces the question/editor/trace workspace.
- T20 should use `--font-mono`, `--color-focus`, the surface and border tokens, and the shared disabled conventions for the code editor. No editor dependency or practice-owned file was changed here.
- Home now presents a 28.8–36px title, explicit baseline status/action, and three compact topic cards. Completed learners can view their baseline status without being promised an unsupported retake or recalibration flow. Baseline examples use two columns on wider screens and return to one column below 600px.

## Visual verification

- Compared against repository `image.png`, then rendered with a local mock API and inspected in headless Chrome at 1440×900 and exact 390×844 CSS viewports.
- Home at 1440×900 fits without page scrolling. Baseline at 1440×900 contains the prompt, examples, constraints, editor, and submit action in one viewport.
- At 390×844, both home and baseline reported `innerWidth = documentElement.scrollWidth = body.scrollWidth = 390`; primary panels end at 374px with 16px page gutters. The long function signature scrolls inside its own code block instead of widening the page.
- The inspected normal-text contrast ratios range from 5.59:1 for subtle text to 16.00:1 for primary text. Accent-button text is 10.20:1; danger and success alert text exceed 8:1.

## Verification

- `npm test` from `frontend/`: 4 passed.
- `npm run build` from `frontend/`: passed.
- `node --test test/*.test.js` from `frontend/`: 8 passed, including T17 initialization and submission recovery coverage plus routing, baseline gating, hints, recording controls, and quota states.
- `git diff --check` over all T18-owned files: passed.
