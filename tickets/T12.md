# Ticket T12: Practice Experience

## Context Digest

Practice serves Target Difficulty or nearest unattempted Question. Run & Check is the only execution action and shows at most two failed cases. Learner quota makes execution/generation read-only while learning material remains visible. Technique Tag is hidden until explicitly revealed. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T10 provides `apiClient`. APIs: `GET /v1/topic/{topicId}/next-question`, `POST /v1/run-check`, `POST /v1/generate`.

## Ticket

Depends on: T10

Files owned: `frontend/src/practice/PracticeScreen.jsx`, `frontend/src/practice/CodeEditor.jsx`, `frontend/src/practice/practice.css`, `frontend/src/practice/usePractice.js`

Interfaces consumed: T10 client and listed API contracts.

Interfaces produced: `<PracticeScreen topicId={string} />`.

Task: Build topic selection, question/editor flow, verdict rendering, generation control, read-only behavior, and tag reveal.

Acceptance criteria: Submits to `/run-check`, displays no more than two failures, and disables execution/generation only when read-only.

## Rules

- Edit only owned files.
- Do not expose Hidden Tests or reference solutions.
