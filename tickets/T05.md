# Ticket T05: Learner State, Quota, Baseline, and Question Selection

## Context Digest

Identity is `X-Learner-Id`. Baseline is once per browser at fixed five scores and resumes at first unanswered Question. Target Difficulty equals Mastery, otherwise nearest unattempted Question. Learner quota is five runs/two generations; Demo quota is 40 executions/20 generations and fails closed. Completed Topics do not repeat. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T01 exports domain contracts. T02 exports learner, mastery, question, attempt, trace, and prepared repositories.

## Ticket

Depends on: T01, T02

Files owned: `backend/learner_state.py`, `backend/quota.py`, `backend/baseline.py`, `backend/question_select.py`, `tests/test_learning_flow.py`

Interfaces consumed: T01 contracts; T02 repositories; baseline/quota/selection rules.

Interfaces produced: `init_learner`, `get_baseline_next`, `submit_baseline`, `get_next_question`, `consume_execution_quota`, `consume_generation_quota` Lambda-callable functions.

Task: Implement learner initialization, baseline progression, quotas, read-only state, and selection.

Acceptance criteria: Tests cover baseline resume, exact/nearest selection, quota exhaustion, and completed Topic behavior.

## Rules

- Edit only owned files.
- Never invoke execution or generation from this ticket.
