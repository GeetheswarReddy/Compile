# Ticket T14: Demo Trace UI

## Context Digest

Demo Trace visibly reports Question provenance, verification retries, and Mastery snapshots for demo mode only. It must be feature-flagged off in the future real-user build. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T10 provides `apiClient`; API is `GET /v1/demo-trace`.

## Ticket

Depends on: T10

Files owned: `frontend/src/trace/DemoTrace.jsx`, `frontend/src/trace/trace.css`, `frontend/src/trace/useDemoTrace.js`

Interfaces consumed: T10 client and demo-trace API response.

Interfaces produced: `<DemoTrace enabled={boolean} />`.

Task: Render visible demo provenance, retry count, and Mastery snapshots behind an explicit feature flag.

Acceptance criteria: Trace renders only when enabled and is absent when disabled.

## Rules

- Edit only owned files.
- Do not call it an agent trace or debug log in learner-facing copy.
