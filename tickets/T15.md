# Ticket T15: Frontend Composition and Amplify Hosting

## Context Digest

Compile's SPA has topic selection, baseline, and practice flows. Practice hosts hints, reflection, and demo-only trace. It deploys through Amplify Hosting and needs an API base URL supplied by the SAM stack. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T11 exports `<BaselineAssessment />`; T12 exports `<PracticeScreen topicId={string} />`; T13 exports hint/reflection components; T14 exports `<DemoTrace enabled={boolean} />`; T09 produces API base URL output.

## Ticket

Depends on: T11, T12, T13, T14

Files owned: `frontend/src/App.jsx`, `frontend/src/app/routes.jsx`, `frontend/amplify.yml`, `frontend/.env.example`

Interfaces consumed: All component interfaces above and API base URL configuration.

Interfaces produced: composed SPA routing and Amplify build configuration.

Task: Compose the three flows, mount feature modules in correct screens, implement trace flag wiring, and configure Amplify build/deployment.

Acceptance criteria: `npm run build` passes; all flows are reachable; trace flag works; Amplify config builds SPA.

## Rules

- Edit only owned files.
- Do not modify upstream feature modules.
