# Ticket T11: Baseline Experience

## Context Digest

Compile is a React/Vite SPA. Baseline is mandatory once per browser: Arrays scores 3 and 6, Strings scores 3 and 6, Hash Maps/Two Pointers score 5. It is one Question at a time, has progress dots and no timer, and resumes at the first unanswered Question. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T10 provides `apiClient` and `getLearnerId()`. API: `GET /v1/baseline/next`, `POST /v1/baseline/submit`.

## Ticket

Depends on: T10

Files owned: `frontend/src/baseline/BaselineAssessment.jsx`, `frontend/src/baseline/baseline.css`, `frontend/src/baseline/useBaseline.js`

Interfaces consumed: T10 client; baseline endpoints.

Interfaces produced: `<BaselineAssessment />`.

Task: Build one-question-at-a-time baseline UI with progress and server-driven resume behavior.

Acceptance criteria: Renders all five steps, resumes correctly, and does not permit bypass.

## Rules

- Edit only owned files.
- Do not edit application routing; T15 owns composition.
