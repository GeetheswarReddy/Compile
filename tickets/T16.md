# Ticket T16: Restore deployed baseline and practice submission

Priority: P0 — release blocker
Status: Ready for investigation
Depends on: None

## Evidence and problem

The user reports that submit does not work in deployment. Repository `image.png` shows the home screen with “Failed to fetch”; it does not show a submission request or establish its root cause. On 2026-09-19 around 08:54 UTC, `compile-v2` was `UPDATE_ROLLBACK_IN_PROGRESS`. CloudFormation recorded SandboxFunction creation failing because reserved concurrency would reduce account unreserved concurrency below 10. The account concurrency limit is 10. Current `infra/template.yaml` already omits that reservation. ReflectionsTable remained `UPDATE_IN_PROGRESS` during rollback; DynamoDB reported TTL enabled on `ttl`. Do not assume the table has failed or force a TTL change.

Amplify's latest inspected main build succeeded at commit `9cc3477`. App-level VITE API URL, demo trace flag, monorepo root, and SPA rewrite were configured correctly. This does not prove the compiled client or browser requests work.

## Files owned

`infra/template.yaml`, `samconfig.toml`, `backend/api_adapters.py`, `backend/dependencies.py`, `backend/http.py`, `backend/baseline.py`, `backend/run_check.py`, `backend/execution_runner.py`, `backend/quota.py`, `tests/test_integration.py`, `scripts/smoke_api.py`, `tickets/out/T16.md`.

## Interfaces consumed

Existing AWS stack `compile-v2` in `ap-southeast-2`; API `https://j8ftz443b9.execute-api.ap-southeast-2.amazonaws.com/v1`; Amplify origin `https://main.d17lwbfv0m80so.amplifyapp.com`. Existing learner identity, baseline, execution, quota, and repository contracts.

## Interfaces produced

Existing `POST /learner/init`, `GET /baseline/next`, `POST /baseline/submit`, and `POST /run-check` contracts restored without route or payload changes. Submission remains `{questionId, code}` with `X-Learner-Id`.

## Work

1. Recheck current stack state and events; recover the existing stack through the appropriate supported operation only if needed. Do not delete/recreate it or skip resources without assessing data and configuration consequences.
2. Reproduce initialization, baseline submit, and practice Run & Check separately. Record sanitized request path, status, response, request ID, and corresponding Lambda logs. Check preflight and actual/error response CORS, deployed handler versions, private executor invocation permissions, execution timeout, and quota denial.
3. Establish the actual failure before changing code. Preserve pending reflection fixes. Keep execution in the isolated Lambda and keep persisted quotas intact; never bypass isolation or reset quotas to make verification pass.
4. Validate/build current sources, review the change set, and update the existing stack after rollback is complete. Follow existing deployment authorization and leave Git pushing to the user.
5. Run a bounded live test with a fresh learner: five baseline submissions and one practice check. Coordinate the same learner/evidence with T21 to avoid duplicating budget consumption.

## Acceptance criteria

- Existing stack reaches `UPDATE_COMPLETE`; final deployed revision and timestamp are recorded.
- From the hosted origin, learner initialization and five baseline submissions succeed, reload resumes progress, and completion permits practice.
- Practice Run & Check returns a visible verdict for the submitted active question/code; quotas and first-attempt mastery rules still hold.
- Success and error responses are readable by the browser; errors are not misreported as missing questions.
- Appropriate backend tests and SAM validation/build pass. Live evidence is required; local tests alone do not close this ticket.
- Report any remaining blocker precisely, without claiming deployment success.
