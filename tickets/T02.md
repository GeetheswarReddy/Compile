# Ticket T02: Persistence Repository Layer

## Context Digest

Compile is a Python 3.12 Lambda/AWS demo. DynamoDB persists learner, Mastery, Question, Attempt, Demo Trace, Reflection metadata, and learner/topic-scoped Prepared Questions; S3 holds audio. Only first attempts change Mastery. Prepared Questions are consumed or discarded when stale. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T01 exports `LearnerState`, `MasteryState`, `Question`, `PreparedQuestion`, `SubmissionVerdict`, `Topic`, and `Provenance`.

## Ticket

Depends on: T01

Files owned: `backend/repositories.py`, `backend/table_names.py`, `tests/test_repositories.py`

Interfaces consumed: T01 contracts; DynamoDB table/key definitions in the Digest.

Interfaces produced: `LearnerRepository`, `MasteryRepository`, `QuestionRepository`, `AttemptRepository`, `TraceRepository`, `ReflectionRepository`, `PreparedQuestionRepository`.

Task: Implement DynamoDB operations, conditional first-scored-attempt writes, and Prepared Question consume/delete semantics.

Acceptance criteria: Tests verify declared keys, conditional scored attempts, and consumed Prepared Questions cannot be returned.

## Rules

- Edit only owned files; do not add Lambda handlers or table IaC.
- Preserve all key shapes stated in the digest.
