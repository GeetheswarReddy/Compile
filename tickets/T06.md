# Ticket T06: Run & Check and Hint APIs

## Context Digest

Run & Check is the only learner execution action. The first verdict alone is scored and changes Topic Mastery by one clamped point; subsequent checks are feedback-only. Hints have four requested levels, reduce Mastery Confidence, and cannot alter a verdict. Demo Trace captures provenance, retries, and Mastery snapshot. Complete digest: `tickets/digest.md`.

## Upstream interfaces

T01 contracts; T02 repositories; T04 `run_submission(question, code) -> SubmissionVerdict`; T05 quota functions and learner state functions.

## Ticket

Depends on: T01, T02, T04, T05

Files owned: `backend/run_check.py`, `backend/hints.py`, `tests/test_run_check.py`, `tests/test_hints.py`

Interfaces consumed: Upstream interfaces above.

Interfaces produced: `run_check(event, context)` and `get_next_hint(event, context)` Lambda handlers.

Task: Implement submission evaluation, first-attempt Mastery update, trace writing, and level-gated hints.

Acceptance criteria: Tests prove later checks do not change Mastery, hints advance one level, and verdicts remain unchanged after hints.

## Rules

- Edit only owned files.
- Delegate every execution to T04's interface.
