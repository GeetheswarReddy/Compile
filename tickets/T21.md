# Ticket T21: Verify and release the redesigned deployed workflow

Priority: P0 release gate
Status: Blocked on implementation tickets
Depends on: T16, T17, T18, T19, T20

## Files owned

`frontend/test/app.test.js`, `README.md`, `tickets/out/T21.md`, `tickets/out/ui-review/`.

## Interfaces consumed / produced

Consume final public frontend/backend contracts and deployment artifacts from T16–T20. Produce a dated release report containing exact backend/frontend revisions, commands/results, sanitized live evidence, screenshots, known limitations, and go/no-go decision. No new product interfaces.

## Acceptance checklist

- Run backend tests, frontend contract/mounted tests, production build, and SAM lint/build as appropriate to final changes. Record the macOS skip for Linux kernel restrictions; require Linux CI evidence before claiming that check passed.
- Check deployed Amplify revision and compiled API destination; verify `/`, `/baseline`, and direct `/practice/arrays` navigation and reload.
- In a real browser on the hosted origin, initialize a fresh learner, complete/resume the five-question baseline, reach practice, type code, click Run & Check, and see the correct result and mastery behavior. Coordinate with T16's six-execution smoke budget; record shared quota before/after and never silently reset it. If more live submissions are required, state the count and available budget before running.
- Exercise network failure and recovery with code preserved, incorrect answer vs infrastructure failure, quota read-only state, completed topic, trace flag off/on, hints, and reflection/history access. Use local fault injection for failure cases rather than deliberately breaking deployed infrastructure.
- Capture home, baseline, and practice screenshots at 1440×900, 1280×720, and 390×844, including populated bottom-left trace and editor result. Compare against `image.png` and the T19 diagram.
- Confirm readable contrast, keyboard operation, visible focus, no overlap/overflow, stable pane sizing, and reachable action controls. Local jsdom tests are insufficient for visual or live-network acceptance.
- Verify audio upload/playback/delete after the pending reflection fixes deploy; use synthetic test audio and avoid collecting personal recordings.
- Do not mark complete while CloudFormation is rolling back, while live submission fails, or without browser evidence. Browser automation was unavailable during ticket creation; record it as a blocker if still unavailable rather than substituting curl as browser proof.
- Keep the stack/data intact and leave `git push` to the repository owner per the project handoff. Record any user push dependency explicitly; a local commit is not a deployed frontend release.
