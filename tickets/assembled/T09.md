# T09
Compile deploys via AWS SAM: API Gateway REST `/v1`, Python 3.12 Lambdas, DynamoDB, S3, Step Functions, Bedrock, and Amplify-consumable API outputs. Execution runner needs 256 MB Lambda config/no network; reflection audio has 30-day lifecycle. Region is ap-southeast-2. Full digest: tickets/digest.md.
Upstream handlers/modules T02–T08 are complete.
Files owned: `infra/template.yaml`, `infra/statemachines/generation.asl.json`, `infra/policies/execution-runner-policy.json`, `samconfig.toml`.
Task: define deployable SAM resources/routes/tables/roles/lifecycle/state machine/config. Run sam validate if installed. Only edit owned files; report interfaces/blockers.
