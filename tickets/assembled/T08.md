# Digest
One private Reflection Recording per learner/question: S3 binary, DynamoDB metadata, expires in 30 days, immediate deletion, and explicit confirmation before replacement. Full: tickets/digest.md.
# Upstream reports
T01 supplies reflection contracts. T02 ReflectionRepository supports get/save/delete and passed full tests.
# T08
Files owned: `backend/reflection.py`, `tests/test_reflection.py`.
Produce: `create_reflection(event, context)` and `delete_reflection(event, context)` handlers.
Task: issue upload metadata, enforce confirmed replacement, calculate expiry and remove metadata/audio.
# Rules
Only edit owned files; do not create S3 infrastructure. Report actual interfaces/new interfaces/blockers in tickets/out/T08-report.md.
