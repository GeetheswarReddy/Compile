# Digest
Identity is X-Learner-Id. Baseline has fixed five Questions/resumes first unanswered. Target Difficulty is Mastery else nearest unattempted. Learner quota is 5 runs/2 generations; Demo quota is 40/20 and fails closed; no repeat after completion. Full: tickets/digest.md.
# Upstream reports
T01 contracts include Question, LearnerState, MasteryState, PreparedQuestion, TestCase, FailedCase. T02 repositories passed 22 tests: Learner/Mastery/Question/Attempt/Trace/Reflection/Prepared repositories with documented key shapes.
# T05
Files owned: `backend/learner_state.py`, `backend/quota.py`, `backend/baseline.py`, `backend/question_select.py`, `tests/test_learning_flow.py`.
Produce: init_learner, get_baseline_next, submit_baseline, get_next_question, consume_execution_quota, consume_generation_quota.
Task: implement initialization, baseline flow/resume, quotas/read-only, selection/completed Topics. Test all rules.
# Rules
Remediation: create `tests/test_learning_flow.py` (it is owned) and run it with `.venv/bin/python -m pytest tests/test_learning_flow.py`. Cover baseline resume, exact/nearest selection, learner/demo quota exhaustion, and completed Topic behavior. Only edit owned files. Do not execute code/generation. Report actual interfaces/new interfaces/blockers in tickets/out/T05-report.md.
