# Digest
React/Vite baseline is mandatory once per browser: five fixed Questions, one at a time, progress dots, no timer, resume first unanswered. Full digest: `tickets/digest.md`.
# Upstream T10 report
Use `apiClient.getBaselineNext(options)` and `apiClient.submitBaseline(body, options)` plus stable `getLearnerId`; Vite build passes.
# T11
Files owned: `frontend/src/baseline/BaselineAssessment.jsx`, `frontend/src/baseline/baseline.css`, `frontend/src/baseline/useBaseline.js`
Produce: `<BaselineAssessment />`.
Task: Implement baseline UI/resumption.
# Rules
Canonical API contract: `GET /baseline/next` and `POST /baseline/submit` return `{ completed: boolean, question: PublicQuestion | null, progress: { answered: number, total: 5 } }`; submit body is `{ questionId: string, code: string }`. `PublicQuestion` has `questionId`, `topic`, `difficultyScore`, `prompt`, `starterCode`, `functionSignature`, `constraints`, and optional `techniqueTag`; never private test/reference fields. Only edit owned files; do not edit routing. Report actual interfaces/new interfaces/blockers in `tickets/out/T11-report.md`.
