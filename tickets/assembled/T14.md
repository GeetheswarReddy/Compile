# Digest
Demo Trace shows provenance, retries, and Mastery snapshots in demo mode only and must be feature-flagged off otherwise. Full digest: `tickets/digest.md`.
# Upstream T10 report
Use `apiClient.getDemoTrace(options)`; Vite build passes.
# T14
Files owned: `frontend/src/trace/DemoTrace.jsx`, `frontend/src/trace/trace.css`, `frontend/src/trace/useDemoTrace.js`
Produce: `<DemoTrace enabled={boolean} />`.
Task: Implement feature-gated trace rendering.
# Rules
Canonical API contract: `GET /demo-trace` returns `{ entries: Array<{ eventTimestamp: string, provenance: "Seeded" | "Generated", verificationRetries: number, masterySnapshot: { topic: string, mastery: number, confidence: number } }> }`. Only edit owned files; learner-facing copy must not call this agent trace/debug log. Report actual interfaces/new interfaces/blockers in `tickets/out/T14-report.md`.
