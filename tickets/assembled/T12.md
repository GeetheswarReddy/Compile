# Digest
Practice serves Target Difficulty or nearest unattempted. Run & Check returns max two failures. Read-only disables execution/generation but not learning content. Technique Tag is learner-revealed. Full digest: `tickets/digest.md`.
# Upstream T10 report
Use `apiClient.getNextQuestion`, `runCheck`, and `generate`; Vite build passes.
# T12
Files owned: `frontend/src/practice/PracticeScreen.jsx`, `frontend/src/practice/CodeEditor.jsx`, `frontend/src/practice/practice.css`, `frontend/src/practice/usePractice.js`
Produce: `<PracticeScreen topicId={string} />`.
Task: Implement selection/editor/verdict/generation/read-only/tag reveal.
# Rules
Only edit owned files; never expose hidden tests/reference solutions. Report actual interfaces/new interfaces/blockers in `tickets/out/T12-report.md`.
