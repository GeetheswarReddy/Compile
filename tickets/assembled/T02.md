# Digest
Compile is AWS React/Vite + Python Lambda. DynamoDB persists learner, mastery, questions, attempts, trace, reflections, and learner/topic Prepared Questions. First attempts alone change Mastery. Full digest: `tickets/digest.md`.
# Upstream T01 report
T01 produced Topic, Provenance, HintLevel, frozen Question, SubmissionVerdict, LearnerState, MasteryState, PreparedQuestion, TestCase, FailedCase, and public serializers. Tests pass.
# T02
Files owned: `backend/repositories.py`, `backend/table_names.py`, `tests/test_repositories.py`
Produce: LearnerRepository, MasteryRepository, QuestionRepository, AttemptRepository, TraceRepository, ReflectionRepository, PreparedQuestionRepository.
Task: Implement DynamoDB persistence with exact documented keys, conditional first-attempt write, and Prepared Question consume/delete. Test keys and consumption.
Remediation required: `.venv/bin/python -m pytest tests/test_repositories.py` currently fails because `PreparedQuestionRepository.get("learner", Topic.ARRAYS)` returns `None` after `save`, and `TraceRepository` lacks `get(key)`. Fix both within owned files and rerun that command.
Approved interface addition: persist `generationFailureCount` on the existing learner/topic Mastery record and expose `MasteryRepository.increment_generation_failure(learner_id: str, topic: Topic) -> int`. Add focused coverage using `.venv/bin/python -m pytest tests/test_repositories.py`.
# Rules
Only edit owned files. Do not change consumed interfaces. `.venv/bin/python` has boto3 and pytest; create and run `tests/test_repositories.py`. Report actual interfaces/new interfaces/blockers in `tickets/out/T02-report.md`.
