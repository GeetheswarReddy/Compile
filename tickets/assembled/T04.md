# Digest
`execution_runner` alone executes untrusted Python: subprocess, five-second timeout, 128 MB address-space cap, no network. Verdict returns pass/fail and maximum two failure cases. Full digest: `tickets/digest.md`.
# Upstream T01 report
Use T01 Question, TestCase, FailedCase, SubmissionVerdict contracts; all tests pass.
# T04
Files owned: `backend/execution_runner.py`, `tests/test_execution_runner.py`
Produce: `run_submission(question: Question, code: str) -> SubmissionVerdict`; `verify_question(question: Question) -> bool`.
Task: Implement isolated execution, timeout/memory cap, redacted verdicts, and focused tests.
# Rules
Only edit owned files. No external judge; no execution in another module. Report actual interfaces/new interfaces/blockers in `tickets/out/T04-report.md`.
