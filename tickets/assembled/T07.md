# T07
Digest: Bedrock generation verifies candidates; max three immediate retries/two-second candidate budgets/eight-second total; stale discards; failure increments count and curated fallback; Prepared Question storage is separate. Full: tickets/digest.md.
Upstream: T02 repositories, T04 verify_question, T05 generation quota/selection, T01 contracts.
Files owned: `backend/generation_orchestrator.py`, `backend/generation_handlers.py`, `tests/test_generation_orchestrator.py`.
Produce: `start_generation(event, context)`, Step Functions definition factory, `handle_generated_candidate(...)`.
Implement and test. Only owned files; report actual interfaces/new interfaces/blockers.
Resolved upstream interface: `MasteryRepository.increment_generation_failure(learner_id: str, topic: Topic) -> int` persists `generationFailureCount`. Remove injected/mutable counter fallback and use this repository interface; rerun focused tests.
