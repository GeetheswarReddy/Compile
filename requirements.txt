# Runtime dependencies are intentionally empty.
# AWS Lambda's Python runtime supplies boto3. Development dependencies are in
# requirements-dev.txt so SAM does not bundle test tooling into every Lambda.
